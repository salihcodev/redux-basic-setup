// LOADING REDUCER FILE:

// import actions
import { SET_LOADING, GET_PRODUCTS } from '../actions/MainActions';

// INITIAL STATE:
const initState = {
  name: 'PRODUCTS',
};

// single source of truth. [REDUCER]
export default function productsReducer(state = initState, action) {
  // check which type of action we need to execute:

  return state;
}
